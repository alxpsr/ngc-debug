{
  "name": "ngc-debug",
  "version": "0.0.7",
  "description": "Lightweight Angular component debugger extension for Chrome",
  "scripts": {
    "build": "rm -rf dist && rollup -c"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/alxpsr/ngc-debug.git"
  },
  "devDependencies": {
    "rollup": "^2.79.2",
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-terser": "^7.0.2"
  }
}
